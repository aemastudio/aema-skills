# AEMA Skills 한국어 사용 매뉴얼

## 1. 무엇을 위한 스킬인가요?

AEMA Skills는 아이디어부터 대본, 이미지 레퍼런스, Seedance 2.5 영상 계획, Blender 보조 제작, 한국어 SRT까지 연결하는 AI 숏드라마 제작 세트입니다.

핵심 원칙은 다음과 같습니다.

- 기본 화면 비율은 `9:16`입니다.
- 캐릭터·장소·소품에 고정 ID와 버전을 부여해 컷 사이의 일관성을 지킵니다.
- 대본 승인과 이미지·영상 생성 승인은 서로 분리합니다.
- 유료 생성, 업로드, Elements 등록은 항상 dry-run 계획을 먼저 보여줍니다.
- 승인되거나 선택된 결과물을 덮어쓰지 않고 새 버전으로 보존합니다.
- 각 결과와 진행 상태를 하나의 project archive에 기록합니다.

## 2. 설치하기

이 저장소는 비공개 저장소이므로 설치하는 컴퓨터에서 `aemastudio/aema-skills`를 읽을 수 있는 GitHub 계정 인증이 필요합니다.

Codex에서 아래와 같이 요청하는 방법이 가장 간단합니다.

```text
$skill-installer로 GitHub 저장소 aemastudio/aema-skills에서 다음 6개 스킬을 설치해줘.

- aema-scriptwriter
- aema-asset-builder
- aema-seedance25-video
- aema-drama-pipeline
- aema-korean-srt
- aema-blender
```

설치가 끝난 뒤 새 대화를 시작하면 스킬이 검색됩니다. 이미 같은 이름의 스킬 폴더가 있다면 설치 프로그램은 덮어쓰지 않으므로, 기존 버전을 확인한 뒤 업데이트해야 합니다.

수동 설치가 필요한 경우 저장소를 내려받고 위의 각 `aema-*` 폴더를 Codex 스킬 디렉터리의 형제 폴더로 복사합니다. 폴더 내부가 중첩되어 `aema-scriptwriter/aema-scriptwriter/SKILL.md` 형태가 되지 않도록 주의하세요. 정상 구조는 `skills/aema-scriptwriter/SKILL.md`입니다.

## 3. 가장 쉬운 사용법: 전체 파이프라인

처음부터 끝까지 제작하려면 통합 스킬을 호출합니다.

```text
$aema-drama-pipeline
설레는 오해와 반전이 있는 60초 로맨틱 코미디 숏드라마를 만들자.
9:16 세로 영상이고, 3명의 얼굴과 의상이 모든 컷에서 일관되어야 해.
지금은 대본, 레퍼런스, 영상 프롬프트 계획까지만 dry-run으로 진행해줘.
```

파이프라인은 필요한 스킬을 다음 순서로 연결합니다.

```text
aema-scriptwriter
        ↓
aema-asset-builder ── 필요 시 aema-blender
        ↓
aema-seedance25-video
        ↓
aema-korean-srt  ← 실제 영상 또는 음성이 생긴 뒤 실행
```

`aema-drama-pipeline`은 다른 스킬의 창작 로직을 복제하지 않습니다. 프로젝트 상태, 호출 순서, 승인 지점, 재개와 버전 이력만 관리합니다.

## 4. 스킬별 사용 예시

### aema-scriptwriter

아이디어를 시놉시스, 회차 구성, 씬별 대본으로 발전시킬 때 사용합니다.

```text
$aema-scriptwriter
소개팅에서 시작된 오해가 설레는 반전으로 뒤집히는 60초 로맨틱 코미디를 써줘.
첫 3초에 강한 훅을 넣고, 장면마다 행동과 대사를 나눠줘.
```

대본에는 `CHAR-001`, `LOC-001`, `PROP-001`, `E01-S001` 같은 고정 ID가 포함될 수 있습니다. 이 ID가 뒤 단계의 레퍼런스와 영상 컷을 연결합니다.

### aema-asset-builder

승인된 대본에서 캐릭터, 배경, 소품을 추출하고 이미지 레퍼런스 프롬프트와 생성 계획을 만듭니다.

```text
$aema-asset-builder
이 승인된 대본을 분석해서 캐릭터·장소·소품 레지스트리와
9:16 제작용 레퍼런스 이미지 프롬프트를 만들어줘.
실제 생성이나 Elements 등록은 하지 말고 dry-run까지만 해줘.
```

실제 생성이 필요하면 계획에 표시된 `run_id`, `plan_hash`, 항목 수, 모델, 크기, 예상 비용을 확인한 뒤 정확한 범위를 승인합니다.

```text
방금 보여준 RUN-... / plan_hash ... 계획에서
CHAR-001 v001 이미지 2장만 생성해줘. 자동 재시도는 하지 마.
Elements 등록은 아직 하지 마.
```

Codex에서는 사용 가능한 이미지 생성 기능으로 승인된 항목만 만들고, Higgsfield 연결 기능이 있을 때 Elements 등록을 별도 승인 단계로 처리합니다. Claude에서는 연결된 Higgsfield MCP를 사용할 수 있지만 동일하게 생성과 등록 범위를 따로 승인해야 합니다.

### aema-seedance25-video

승인된 씬과 잠긴 레퍼런스를 Seedance 2.5 컷 프롬프트로 변환합니다.

```text
$aema-seedance25-video
승인된 E01-S001을 9:16 영상 컷으로 나눠줘.
캐릭터 레퍼런스 버전은 CHAR-001/v002로 고정하고
영상 생성 없이 프롬프트와 비용 계획까지만 작성해줘.
```

영상 제출 전에는 컷·테이크 ID, 모델, 길이, 해상도, 참조 이미지, 작업 수, 예상 비용과 최대 시도 횟수를 확인합니다. 유료 리테이크는 자동 실행되지 않습니다.

### aema-blender

정확한 카메라, 공간 배치, 조명, 반복 렌더 또는 3D 소품이 필요한 장면에 선택적으로 사용합니다.

```text
$aema-blender
E01-S003 거실 장면을 9:16 기준으로 구성해줘.
인물 두 명의 위치, 카메라, 조명과 렌더 설정을 계획하고
Blender 실행 전까지 dry-run으로 멈춰줘.
```

먼저 저해상도 preview를 검토하고, 전체 해상도 또는 장시간 render는 별도로 승인하는 흐름을 권장합니다. 이 스킬은 기존 Blender 씬 전체를 지우거나 관련 없는 오브젝트를 자동 삭제하지 않습니다.

### aema-korean-srt

실제 영상이나 음성이 준비된 뒤 한국어 SRT를 생성합니다. 설치되어 있는 로컬 Whisper 모델을 우선 사용하고, 모델을 자동 다운로드하거나 유료 서비스로 자동 전환하지 않습니다.

```text
$aema-korean-srt
final.mp4에서 한국어 자막을 만들어줘.
먼저 로컬 Whisper와 모델 사용 가능 여부를 확인하고 dry-run 결과를 보여줘.
말뜻은 바꾸지 말고 읽기 편한 띄어쓰기와 문장부호만 정리해줘.
```

결과에는 `.srt`, 기계 판독용 transcript 정보, QC 보고서가 포함됩니다. 자막을 영상 화면에 입히는 작업은 별도의 영상 편집 작업입니다.

## 5. 프로젝트 archive

전체 파이프라인을 사용하면 한 작품의 자료를 아래 구조로 관리합니다.

```text
PROJECT_ROOT/
├─ aema-project.json
├─ 01_script/       대본, 트리트먼트, 설정
├─ 02_references/   캐릭터, 장소, 소품 레퍼런스
├─ 03_prompts/      이미지, 영상, Blender 프롬프트·계획
├─ 04_elements/     Higgsfield Elements 등록 계획과 기록
├─ 05_video/        생성 컷, 선택본, QC
├─ 06_blender/      씬 계획, 스크립트, preview, render
├─ 07_audio/        원본 및 추출 오디오
├─ 08_subtitles/    transcript, SRT, QC
├─ 09_exports/      납품 파일
└─ runs/            실행별 계획, 승인, 상태 기록
```

새 archive를 시작하는 예시:

```text
$aema-drama-pipeline
새 작품 제목은 "우리, 소개팅 맞죠?"야.
9:16, 한국어 프로젝트 archive를 만들 위치와 생성될 구조를 먼저 보여줘.
확인 전에는 실제 폴더를 만들지 마.
```

dry-run 결과를 확인한 뒤 다음처럼 실행을 요청할 수 있습니다.

```text
방금 보여준 위치에 프로젝트 archive를 실제로 만들어줘.
```

`aema-project.json`은 파이프라인만 갱신합니다. 개별 스킬은 자신의 결과 조각만 반환하므로 동시에 서로의 상태를 덮어쓰지 않습니다.

## 6. 승인 규칙과 크레딧 절약

다음 요청은 계획만 만들며 비용을 사용하지 않는 것이 기본입니다.

```text
dry-run으로 분석과 프롬프트만 작성해줘.
외부 생성, 업로드, Elements 등록, 유료 재시도는 하지 마.
```

실행 전에는 최소한 아래 내용을 확인하세요.

- `run_id`와 `plan_hash`
- 생성할 정확한 이미지·컷·테이크 목록
- 서비스와 모델
- 이미지 크기 또는 영상 길이·해상도
- 총 작업 수와 예상 비용 또는 `unknown`
- 업로드·Elements 등록 같은 외부 변경
- 최대 시도 횟수

모델, 개수, 해상도, 길이, 레퍼런스 또는 비용 상한이 바뀌면 기존 승인은 무효가 되고 새 계획을 검토해야 합니다.

## 7. 중간부터 다시 시작하기

프로젝트가 중단됐다면 기존 archive를 지정하고 재개를 요청합니다.

```text
$aema-drama-pipeline
이 AEMA 프로젝트 archive의 현재 상태를 읽고 이어서 진행해줘.
완료된 결과는 재생성하지 말고, 막힌 단계와 다음 승인 항목만 알려줘.
```

대본이나 레퍼런스가 바뀌면 영향을 받는 후속 결과만 `stale`로 표시하고 이전 버전은 삭제하지 않습니다.

## 8. 자주 생기는 문제

### 스킬이 보이지 않아요

- 여섯 폴더 각각의 바로 아래에 `SKILL.md`가 있는지 확인합니다.
- 설치 직후라면 새 대화에서 다시 호출합니다.
- 이름을 `$aema-drama-pipeline`처럼 정확히 입력합니다.

### 비공개 저장소에서 설치가 안 돼요

- 설치하는 컴퓨터가 올바른 GitHub 계정으로 인증되어 있는지 확인합니다.
- 해당 계정에 `aemastudio/aema-skills` 읽기 권한이 있어야 합니다.
- 인증 토큰이나 비밀번호를 대화, archive, 커밋에 저장하지 마세요.

### Higgsfield Elements 등록이 멈췄어요

연결된 도구가 기존 Element 사용만 지원하고 새 Element 등록을 지원하지 않을 수 있습니다. 이 경우 스킬은 가짜 ID를 만들지 않고 수동 등록용 manifest를 준비한 뒤 `blocked`로 표시합니다.

### 영상이나 이미지가 자동 생성되지 않아요

정상적인 안전 동작일 수 있습니다. 프롬프트 작성과 대본 승인은 외부 생성 권한이 아닙니다. 표시된 실행 계획의 정확한 범위를 별도로 승인하세요.

### SRT 생성이 막혔어요

실제 미디어 파일, 로컬 Whisper 패키지, 기존 로컬 모델 파일이 모두 필요합니다. 네트워크 모델 다운로드와 유료 전사는 자동으로 실행되지 않습니다.

## 9. 권장 첫 요청

```text
$aema-drama-pipeline
소개팅에서 시작된 오해와 반전을 다룬 60초 한국어 로맨틱 코미디를 만들자.
9:16 세로 영상이며 반복 등장인물의 얼굴, 의상, 소품 일관성이 중요해.

1. 아이디어를 3개 제안하고
2. 내가 고른 아이디어만 대본으로 발전시키고
3. 대본 승인 뒤 레퍼런스 계획을 만들고
4. 영상 생성 전 프롬프트와 비용 범위를 보여줘.

모든 외부 이미지·영상 생성과 Elements 등록은 dry-run으로 멈추고
내가 항목별로 승인할 때만 실행해줘.
```
